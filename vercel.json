{
  "version": 2,
  "buildCommand": "npm run build -- --configuration=production",
  "outputDirectory": "dist/hr-dashboard",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
